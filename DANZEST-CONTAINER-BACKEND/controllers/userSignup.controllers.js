/** TODO - 
 * check if 
     - The admin is already exist and should be one admin account. If the admin is exist throw an error - admin is already exist otherwise admin is successfully registered .
     
     - the regular user is exist. ifExist, throw error. Otherwise, user is successfully registered.
     NB: hashedPassword and upload profilePicture will be done on middleware and pass into routes
*/

exports.signup = async (req, res, next) => {
  const {} = req.body;
  //TODO
  try {
    //TODO
  } catch (err) {
    //TODO
  }
};
